---
- name: Play to create partition , format file system and mount it
  hosts: localhost
  vars:
    - partition_number: 1
    - device_path: /dev/sdb
    - mount_point: /mostafa
    - filesystem_type: ext4
    - part_start: 1MB
    - partition_size: 400MB
  tasks:
    - name: New partition of {{partition_size}}
      import_tasks: makepartition.yml
    - import_tasks: filesystemtomount.yml

